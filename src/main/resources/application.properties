spring.application.name=QuanLyBanVeMayBay

server.address=0.0.0.0
server.port=${PORT:8080}
server.servlet.session.persistent=false

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/airline_booking_system}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:dat1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:tle723772@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:tzrbhqokiajeaydr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.thymeleaf.cache=false

# Show stacktrace in error page for debugging
server.error.include-stacktrace=always
server.error.include-message=always
server.error.include-exception=true